# LangGraph agent framework
langgraph>=0.2.0
langchain-openai>=0.2.0
langchain-google-genai>=2.0.0

# Web search
tavily-python>=0.5.0

# Experiment tracking (optional — graceful fallback if not installed)
mlflow>=2.0.0

# Environment
python-dotenv>=0.5.1
